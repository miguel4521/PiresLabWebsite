const Header = ({header}) => {
    return <div className="page-container">
        <div className="content-container">
            <div className="header-txt">{header}</div>
        </div>
    </div>;
}

export default Header;